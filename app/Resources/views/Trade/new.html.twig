{% extends "::base.html.twig" %}

{% block body %}
  <div class="row">
    <div class="col-lg-6">
      {{ form_start(form) }}

        <div class="form-group">
          <label>Libellé du portefeuille</label>
          {{ form_widget(form.name,{'attr':{'class':'form-control'}}) }}
        </div>

        <div class="form-group">
          <div class="radio">
            {{ form_widget(form.public[0]) }}
          </div>

          <div class="radio">
            {{ form_widget(form.public[1]) }}
          </div>
        </div>

      {{ form_end(form) }}
    </div>
  </div>
{% endblock %}
