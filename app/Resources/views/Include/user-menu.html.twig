<li class="nav-header">
    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
      <div class="dropdown profile-element">
          <a data-toggle="dropdown" class="dropdown-toggle" href="#">
          <span class="clear"> <span class="block m-t-xs"> <strong class="font-bold">{{ app.user.username }}</strong>
          </span> <span class="text-muted text-xs block">{{ 'user'|trans }}<b class="caret"></b></span> </span> </a>
          <ul class="dropdown-menu animated fadeInRight m-t-xs">
              <li><a href="{{ path('fos_user_profile_show')}}">{{ 'profil'|trans }}</a></li>
              <li class="divider"></li>
              <li><a href="{{ path('fos_user_security_logout')}}">{{ 'logout'|trans }}</a></li>
          </ul>
      </div>
    {% endif %}
</li>
